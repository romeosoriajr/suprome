{
  "name": "suprome",
  "private": true,
  "scripts": {
    "dev": "npx @11ty/eleventy --config=eleventy.config.js --input=./src --output=dist --serve",
    "build": "npx @11ty/eleventy --config=eleventy.config.js --input=./src --output=dist"
  },
  "dependencies": {},
  "devDependencies": {
    "@11ty/eleventy": "^0.11.0"
  }
}
